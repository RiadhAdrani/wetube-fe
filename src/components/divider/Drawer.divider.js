import { Hr } from "@riadh-adrani/recursive-web/html";
import { Var } from "../../util/Style";

export default Hr({
    width: "90%",
    size: "1px",
    nstyle: { scoped: true, normal: { color: Var("invert9") } },
});
